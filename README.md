CBOE Pitch protocol implementation
==================================

This repo contains code generation to produce pitch protocol parser in rust. I'm not terribly happy with it, especially the code generator, but it seems to work.  
